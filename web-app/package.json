{
  "name": "dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:api": "npm --prefix ../tools/openapi run generate",
    "check:api": "npm --prefix ../tools/openapi run check",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "test": "vitest run",
    "check:artifact": "python3 ../scripts/check-web-artifact.py",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^24.12.2",
    "eslint": "^9.39.4",
    "fake-indexeddb": "^6.2.5",
    "globals": "^17.4.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.58.0",
    "vite": "^8.0.4",
    "vitest": "^4.1.0"
  }
}
